import { Component } from '@angular/core';

@Component({
    selector: 'app-register',
    standalone: true,
    template: `
        <h1>Inscription</h1>
        <p>Formulaire d'inscription (bientôt).</p>
    `
})
export class RegisterComponent {}
