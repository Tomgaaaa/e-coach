import { Component } from '@angular/core';

@Component({
    selector: 'app-login',
    standalone: true,
    template: `
        <h1>Connexion</h1>
        <p>Formulaire de connexion (bientôt).</p>
    `
})
export class LoginComponent {}
